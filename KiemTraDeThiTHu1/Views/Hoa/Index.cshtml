@model IEnumerable<KiemTraDeThiTHu1.models.Hoa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<p>
    <a asp-action="RollBack">RollBack </a>
</p>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenHoa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThucVat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenHoa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThucVatId)
            </td>
            <td>
                <a asp-action="Update" asp-route-id="@item.HoaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HoaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HoaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
